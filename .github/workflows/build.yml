name: Build CI
on:
  workflow_dispatch:
    inputs:
      link:
        description: "Enlace"
        required: true
      sukisu:
        description: "true"
        required: true
        default: "true"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch
        run: |
          curl -L "${{ github.event.inputs.link }}" -o boot.img
          curl -L "https://github.com/bmax121/KernelPatch/releases/download/0.12.4/kptools-linux" -o kptools
          chmod +x kptools
          ./kptools -g kpimg.elf
          ./kptools -i boot.img -p kpimg.elf --out boot-patched.img
      - uses: actions/upload-artifact@v4
        with:
          name: boot-patched
          path: boot-patched.img
