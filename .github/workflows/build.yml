name: Build CI
on:
  workflow_dispatch:
    inputs:
      link:
        description: "Pega aqui tu link de LimeWire"
        required: true
      sukisu:
        description: "Escribe la palabra true"
        required: true
        default: "true"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Patch Boot
        run: |
          wget -O boot.img "${{ github.event.inputs.link }}"
          wget https://github.com/bmax121/KernelPatch/releases/download/0.12.4/kptools-linux
          chmod +x kptools-linux
          ./kptools-linux -g kpimg.elf
          ./kptools-linux -i boot.img -p kpimg.elf --out boot-patched.img
          
      - name: Upload Result
        uses: actions/upload-artifact@v4
        with:
          name: boot-patched
          path: boot-patched.img
